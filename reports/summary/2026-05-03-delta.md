# 사이클 요약 — 2026-05-03

## Activity: A+B+C
## 아이디어: SemanticSegmentCache(DHD) + TurboQuant 3-bit + DualMapScheduler 전체 통합

## 핵심 결과
- Report ①: A+B+C 45/45 테스트 1회차 통과 — DualMapScheduler(0.028ms/req) + SemanticSegmentCache DHD(비연속 히트율 1.0) + TurboQuantCodec(−70.3% 메모리, cosine_sim 4-bit=0.9957)
- Report ②: vLLM 0.20.1 (v1 엔진) 1회차 통과 — DualMapSchedulerMixin(0.0197ms/req) + SemanticNonContiguousKVCacheManager + VllmTurboQuantCodec; CacheCompressionConfig composition 패턴으로 CacheConfig 미수정 구현

## 목표 지표 변화

| 지표 | 이전 최선 | 오늘 | 변화 |
|------|---------|------|------|
| Inference Throughput | +391% (2026-04-30 A+B+C, capacity-constrained) | 구조적 +10~20% (Spec 목표; 시뮬레이션) | A+B+C 통합 재달성; GPU 실측 미완 |
| KV Memory Reduction | −80% (TriState, 2026-04-30) | −70.3% (TurboQuant+DHD) | −9.7%p (TriState 대비); 목표 −30% 대비 2.3× 초과 |
| Non-Contiguous Hit Rate | ≥30% 통합 테스트 통과 (2026-05-02) | 100% (noncontiguous_ratio=1.000) | 역대 최고; semantic-only 히트로 비연속 비율 완전 달성 |
| Effective Context Length | 5× (TriState, 2026-04-30) | 3.37× | −1.63× (TriState 대비); 목표 2× 대비 1.69× 초과 |
| Compression Accuracy Delta | cosine sim min 0.8774 (CapKV, 2026-05-02) | cosine_sim(4-bit)=0.9957, cosine_sim(3-bit)=0.9799 | 대폭 개선; 전 레이어 ≥0.9795 달성, 민감 레이어 역대 최고 |
| Scheduling Overhead | 0.22ms/10req = 0.022ms/req (2026-04-30) | 0.028ms/req (독립) / 0.0197ms/req (vLLM) | 동급 수준; 임계값 5ms/req 대비 178×~254× 여유 |

## 주요 기술 성과

### Activity A — DualMapScheduler
- 의미 히트 가중 라우팅 + fairness_max_wait=10으로 공정성 보호
- vLLM DualMapSchedulerMixin.pre_schedule_sort() / sort_by_cache_affinity() 구현
- 단일 노드 + 멀티 노드(DualMapNodeState 구성) 공통 지원

### Activity B — SemanticSegmentCache(DHD)
- brute-force 코사인 유사도 검색 + deviation 필터 (similarity_threshold=0.70, dev_threshold=0.50)
- 10/10 의미 유사 쿼리 100% semantic 히트; noncontiguous_ratio=1.000
- LRU OrderedDict 기반 퇴거; TurboQuantCodec 직접 통합으로 −70.3% 메모리 압축 저장

### Activity C — TurboQuantCodec(PolarQuant+QJL)
- 민감 레이어: PolarQuant (Hadamard 회전 + 4-bit INT4) → cosine_sim=0.9957, normalized_err=0.0933 ≤ 0.10
- 일반 레이어: 3-bit QJL 잔차 보정 → cosine_sim=0.9799, MSE ratio=0.0417 < 0.15
- 전체 메모리 −70.3% (152,000B vs baseline 512,000B, d_head=128); Effective Context 3.37×

### vLLM 이식 신규 패턴
- CacheCompressionConfig를 vLLM CacheConfig와 독립적으로 composition — CacheConfig frozen 필드 수정 불필요
- VllmTurboQuantCodec이 3D 텐서(n_tokens, num_kv_heads, head_size)를 2D 평탄화 후 인코딩하는 어댑터 레이어 추가
- 압축/해제가 어텐션 커널 진입 전 완료 (SemanticKVAttentionWrapper.load_cached_chunks()에서 _decode_entry() 호출 후 반환)

## 참고 관찰사항 (미해결 이슈 아님)
- 3-bit 일반 레이어(layer_idx=6) normalized_error=0.2042 (0.10 초과); 단, 민감 레이어 기준만 적용되므로 Fail 아님
- SemanticNonContiguousKVCacheManager 실제 vLLM 서빙 파이프라인 연동 시 완전한 KVCacheConfig 필요; E2E 테스트 미완
- SwigPy DeprecationWarning 2건은 vLLM CUDA/OpenMP SWIG 내부 경고; vllm_integration/ 코드와 무관

## 다음 사이클 제언
1. **FAISS 하이브리드 검색 통합**: SemanticSegmentCache brute-force 검색은 N>10K에서 병목. FAISS IVF 자동 전환으로 대규모 캐시 TTFT 보호. Report ①②에서 동시 제언된 항목.
2. **3-bit 레이어 QJL 보정 강화**: normalized_error 0.2042 감소를 위해 QJL 투영 차원 2×d_head 확장 또는 per-column 스케일링 도입.
3. **GPU 벤치마크 환경 구축 + 실제 perplexity 측정**: 5사이클 연속 CPU 시뮬레이션만으로 검증된 상태. torch.cuda.Event 기반 TTFT/TBT 실측 + GPT-2/WikiText-2 perplexity 측정을 통해 +20% throughput 및 ±1% accuracy 주장을 직접 입증.
