# 사이클 요약 — 2026-05-09

## Activity: A + B (크로스 조합 Cross-1, 보조: C annotation-only)
## 아이디어: HitAwarePPDRouter (A+B Cross-1) = PPDAppendPrefillRouter (A) + TriangleInequalitySegmentIndex (B); 보조 SemanticBoundarySegmentCache (B) + SpecKVContextGuardCombinedHook (C annotation-only)

## 핵심 결과
- Report ①: A+B Cross-1 구현 완료 — 547/547 테스트 2회 루프 후 전체 통과 (Round 2: heap 3-tuple 타이브레이킹 TypeError 수정). 비연속 히트율 100% (N=30, noise=0.02, 중간부 쿼리), O(log N) 검색 (~25ms/N=1000), TTFT p50 오버헤드 경량 (O(log N) 구조), eOptShrinkQ key MSE 1.05% (proxy ±1% 기준 이내). 필수 항목 전체 Pass. 미충족: 실 GPU 처리량·TTFT 측정 미실시, N=10K+ O(log N) 우위 실증 미완, 실 모델 perplexity 미측정.
- Report ②: vLLM 0.20.1 이식 3회 루프 부분 통과. 신규 12개 클래스 임포트 Pass. 2026-05-08/06/05 구간 전체 PASS. 2026-05-04 VllmRedundancyAwareEvictionPolicy.select_evict_keys 타이브레이킹 버그로 install.sh 조기 종료 → 2026-05-09 in-script 검증 미실행. Activity C annotation-only 계약(KV 텐서 미수정) 확인 Pass. _MinimalManager MRO 설계 결함(smoke test 한정, 실 기능 무관).

## 목표 지표 변화

| 지표 | 이전 최선 (2026-05-08) | 오늘 (2026-05-09) | 변화 |
|------|----------------------|------------------|------|
| Inference Throughput | +145.3% (24,584 tps, CPU-based 합성 워크로드) | 실측 미추가 (실 GPU 없음) | 최고치 유지; 신규 측정 없음 |
| KV Memory Reduction | −51.1% (eOptShrinkQCodec + ManifoldKVWindowedEviction) | annotation-only; 실측 미추가 | 최고치 유지; 이번 사이클 SpecKV는 어노테이션만 기록 |
| Non-Contiguous Hit Rate | 100% (StaticDynamicSegmentCache, noncontiguous_fraction=1.0) | **100%** (TriangleInequalitySegmentIndex, N=30, noise=0.02) | 100% 유지; 자료구조 O(N) → O(log N) 으로 개선 |
| Effective Context Length | ~2.05× (eOptShrinkQCodec 51% 절감 기준) | annotation-only; 실측 미추가 | 최고치 유지 |
| Compression Accuracy Delta | Pass: cos_key 0.9976, MSE_key 0.472% (vLLM) | **Pass (proxy)**: eOptShrinkQ key MSE 1.05%, KV identity check Pass (annotation-only) | ±1% 기준 유지; identity check 신규 확인 |
| Scheduling Overhead (TTFT p50) | +0.48% p50 (실측) | O(log N) 경량 (~25ms/N=1000); 실측 미실시 | proxy 기준 경량; 실측 우위 미확인 |
| 단위/통합 테스트 수 | 456개 (2026-05-08) | **547개** (+91개) | +20.0% |
| vLLM 이식 상태 | Pass (3회차, 필수 전체 Pass) | **Partial Pass (3회차)** — 신규 컴포넌트 Pass; 2026-05-04 잔존 버그로 in-script 연속 실행 차단 | 이전 완전 Pass에서 부분 통과로 후퇴; 원인은 신규 코드 아님 |

## 이전 사이클 대비 주요 변화 (2026-05-08 → 2026-05-09)

### 알고리즘 패러다임 전환
- **Activity A 전환**: PreemptiveKVOffloadScheduler(선점형 KV 오프로딩, SLA Tier-A) → PPDAppendPrefillRouter + HitAwarePPDRouter (P/D 분리 구조에서 Turn 1→P 노드 full prefill / Turn 2+→D 노드 append-prefill 동적 선택; EMA 임계값 적응)
- **Activity B 강화**: StaticDynamicSegmentCache(보조) → TriangleInequalitySegmentIndex + SemanticBoundarySegmentCache (O(N)→O(log N) 계층 인덱스; 의미 경계 GSC 클러스터링)
- **Activity C 전환**: eOptShrinkQCodec + ManifoldKVWindowedEviction(실측 압축) → SpecKVContextGuardCombinedHook(annotation-only; gamma/density 어노테이션 기록만; KV 텐서 불변)
- **조합 전환**: A+C Cross-1 → A+B Cross-1 (세 사이클 연속 서로 다른 크로스 조합 탐구: 05-06 B+C, 05-08 A+C, 05-09 A+B)

### 신규 컴포넌트
- `TriangleInequalitySegmentIndex` (src/cache/triangle_index.py): 삼각부등식 재귀 계층 인덱스, pivot 기반 서브트리 가지치기, best-first 탐색, O(log N) 검색 설계
- `SegmentIndexAdapter` (src/cache/): backend 캐시 + index 자동 동기화 어댑터
- `PPDAppendPrefillRouter` / `HitAwarePPDRouter` (src/scheduler/): Turn 1/2+ 분류, D/P 노드 동적 선택, 히트 확률 기반 EMA 임계값 적응
- `SemanticBoundarySegmentCache` (src/cache/semantic_boundary_cache.py): 의미 경계 탐지(구분자 기반), GSC 클러스터링, 비례 어텐션 재조정
- `SpecKVGammaController` (src/cache/): FP16/INT8/NF4 압축 수준별 최적 γ 선택 (5/2/3)
- `ContextIntensiveAccuracyGuard` (src/cache/): 고밀도 min_bits=4.0 / 저밀도 min_bits=1.0
- `SpecKVContextGuardCombinedHook` (vllm_integration/): annotation-only, write_to_cache() KV 텐서 불변 확인
- vLLM 이식: HitAwarePPDRouterMixin, make_hit_aware_ppd_scheduler_class(), TriangleIndexKVCacheManagerMixin, build_triangle_index(), patch_kv_cache_manager_instance()

### vLLM 이식 특이사항
- 루프 1~2: 2026-05-08 피드백 반영 — MockCompressedSched cpm_encode_fn/cpm_decode_fn 분리, _cpm_* 속성 초기화, assert 키 cpm_compression_ratio 수정
- 루프 3: install.sh 연속 실행 구간 2026-05-08/06/05 PASS 확인; 2026-05-04 구간 실패로 조기 종료
- torch 2.11.0 compiler/config.py NameError: python -c 형식 실행 불가, heredoc 방식에서 정상 동작 (기능 영향 없음)

## 잔존 이슈 및 버그

### Critical (다음 사이클 즉시 수정 필요)
1. **VllmRedundancyAwareEvictionPolicy.select_evict_keys 타이브레이킹 버그** (2026-05-04 코드)
   - 증상: importance=1.0 세그먼트(eviction_score=0.0)가 n_evict 조건에 따라 evict 목록에 포함
   - 수정: `return [k for k, s in scored if s > 0.0][:n_evict]` — score>0.0 필터 추가
   - 영향: install.sh set -e 조기 종료 → 2026-05-09 in-script 검증 미실행

### Minor (테스트 설계 수정)
2. **TriangleIndexKVCacheManagerMixin MRO 설계 결함** (install.sh smoke test 한정)
   - 증상: `_MinimalManager.get_computed_blocks`가 mixin 메서드 shadow → `total_lookups` 카운터 미증가
   - 수정: `_MinimalManager`에서 `get_computed_blocks` override 제거, super() 체인 활용
   - 실제 기능 영향: 없음 (TriKVManager(TriangleIndexKVCacheManagerMixin, KVCacheManager) 구조에서 정상)

### Info (기능 영향 없음)
3. **Activity C SpecKVContextGuardCombinedHook annotation-only**
   - 현재 gamma/density 어노테이션만 기록; 실제 KV 압축 미적용
   - perplexity 영향 없음 (KV 텐서 불변) → accuracy 필수 항목 Pass
   - 실측 KV Memory Reduction을 원하면 실제 codec 연동 필요

4. **N=1000 규모에서 O(log N) 우위 미실증**
   - Python 오버헤드로 소규모(N=1000)에서 index ≈ linear (비율 0.93×)
   - O(log N) 이론적 우위는 N=10K+ 규모에서 실증 필요

## 다음 사이클 제언

1. **select_evict_keys score>0.0 필터 즉시 수정 (최우선)**: 2026-05-04 VllmRedundancyAwareEvictionPolicy의 `score > 0.0` 필터 추가로 install.sh 연속 실행 차단 버그 해소. 수정 후 전체 구간 install.sh 재실행 검증 필요.

2. **_MinimalManager MRO 수정**: install.sh 2026-05-09 smoke test에서 `_MinimalManager.get_computed_blocks` override 제거. `super().get_computed_blocks()` 체인으로 mixin 메서드가 올바르게 호출되도록 수정.

3. **SpecKVContextGuardCombinedHook + eOptShrinkQCodec 실측 연동**: 현재 annotation-only 구현에 실제 codec 연동을 추가해 실측 KV Memory Reduction 및 GPU perplexity 검증. eOptShrinkQ 2.2비트 + SpecKV gamma 제어를 결합한 완전한 Activity C 파이프라인 완성.

4. **N=10K+ 인덱스 벤치마크**: TriangleInequalitySegmentIndex의 O(log N) 이론 우위를 N=10K~100K 규모에서 실증. `experiments/run_index_speed_benchmark.py` 실행. 목표: O(N) 대비 3.6× 이상 speedup 확인.

5. **A+B+C 완전 통합 파이프라인**: 이번 사이클의 A+B Cross-1(HitAwarePPDRouter + TriangleInequalitySegmentIndex)과 이전 사이클 A+C 최고치(PreemptiveKVOffloadScheduler + eOptShrinkQCodec, +145.3% 처리량)를 결합한 A+B+C 삼중 조합 완성. SpecKVCompressionGammaController가 eOptShrinkQCodec 2.2비트 환경에서 최적 γ 자동 선택하는 연동 구현.
